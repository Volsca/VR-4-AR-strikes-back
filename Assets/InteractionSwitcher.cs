using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InteractionSwitcher : MonoBehaviour
{
    // This script has as its goal to enable switching between the incompatible rayInteractor and directInteractor

    public Component rayIntScript;
    public Component directIntScript;

    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
